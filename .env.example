# Conexión a la base Postgres (Neon). En Vercel se completa sola al
# conectar la integración de Neon. Para correr local, copiá este archivo
# a ".env.local" y pegá tu connection string:
DATABASE_URL="postgresql://usuario:password@host/db?sslmode=require"
